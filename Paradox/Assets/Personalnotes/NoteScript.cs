using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//public=Everyone/Unity can see it/ Class=Class(cookie cutter)
//"NoteScript"=the name you gave the script that says what it do.
//(:=instantiate) MonoBehaviour-is a component that can be added to GameObjects
//{}=scripting home) (scripts are introverts they must have their currlyBrakets!
public class NoteScript : MonoBehaviour {

	// Use this for initialization. (Void=function) Start=happens play is pressed
	void Start () {
		
	}
	
	// Update is called once per frame. Update is constantly happening(blablabla...)
	void Update () {
		
	}
}
